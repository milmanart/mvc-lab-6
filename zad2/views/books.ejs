<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel="stylesheet" type="text/css" href="/css/styles.css">
</head>
<body>
<h1>Books</h1>
<% if (userId) { %>
    <ul>
        <% books.forEach(book => { %>
            <li>
                <a href="/books/<%= book.id %>"><%= book.title %></a> by <%= book.author %>
            </li>
        <% }) %>
    </ul>
    <a href="/user/set">Log out</a>
<% } else { %>
    <p>Sorry, you cannot access library without login into the system.</p>
    <a href="/user/set">Log in</a>
<% } %>
</body>
</html>
