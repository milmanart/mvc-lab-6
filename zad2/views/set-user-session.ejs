<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel="stylesheet" type="text/css" href="/css/styles.css">
</head>
<body>
<h1>Set User</h1>
<% if (users.length > 0) { %>
    <form action="/user/set" method="POST">
        <div>
            <label>User</label>
            <select name="userId">
                <% users.forEach(user => { %>
                    <option value="<%= user.id %>"><%= user.login %></option>
                <% }) %>
            </select>
        </div>
        <button>Set user</button>
    </form>
<% } else { %>
    <p>Sorry, no users has been found.</p>
<% } %>
<a href="/">Back to Home</a>
</body>
</html>
